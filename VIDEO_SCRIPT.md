# VIDEO SCRIPT — moved

> **This file has moved to [`docs/video_script.md`](docs/video_script.md).**
> Shooting checklist: [`docs/video_checklist.md`](docs/video_checklist.md).

As of Sprint 4 the video script lives in `docs/` and is a **4-minute, three-agent** version
(features the **Reviewer** and keeps the MCP-call counter on screen). This root stub is kept
only so existing links don't 404 — do not edit it; edit the canonical file instead.

The old 5-minute 2-agent version (deploy-focused) is superseded and available in git history.
